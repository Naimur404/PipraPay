services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${APP_PORT:-80}:80"
    environment:
      - DB_HOST=eg8zvrpkwk118e6qrzedm7v2
      - DB_PORT=3306
      - DB_NAME=payment
      - DB_USER=mariadb
      - DB_PASS=XMIyY6EitnLtPqENMcwX8gcuF8jeV4zpLPeVCUVUlmnR39FDAOmybBJeZuJriaLZ
    volumes:
      - app_media:/var/www/html/pp-media
      - app_config:/var/www/html/pp-content
    restart: unless-stopped

volumes:
  app_media:
  app_config:
